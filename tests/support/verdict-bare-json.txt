{"priority": "low", "tier": 2, "rationale": "The code confirms this is a capability request, not a defect: lib/sandbox/stats.sld exposes only mean, median, and sum-of, and there is no CLI, flag parsing, or JSON anywhere in the repo (grep for json/flag/output/cli finds nothing), so the '--json flag' framing names a component that doesn't exist — but the underlying ask, emitting results as JSON, is still purely additive. Existing documented behaviour (docs/usage.md documents the three procedures; the test harness prints its own PASS/FAIL lines) is unaffected, and nothing in the repository or its consumers depends on JSON output. This lands squarely under §4, 'requests for new capability that nothing currently depends on', which assigns priority low, while the reporter's own framing ('new capability rather than a defect', 'not urgent') matches §5's feature-request category, which mandates tier 2: the shape of the feature (library-level serializer vs. a CLI wrapper the repo doesn't have) is an open design decision, so a human must decide the work is wanted before it is done."}